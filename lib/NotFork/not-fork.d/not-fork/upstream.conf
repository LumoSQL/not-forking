# upstream configuration for not-fork (so it can not-fork itself)

vcs   = fossil
repos = https://lumosql.org/src/not-forking

# version numbers have the form "version-N.NN.NN" so we indicate the
# prefix "version-" here and use the "N.NN.NN" as version number
version = version-

